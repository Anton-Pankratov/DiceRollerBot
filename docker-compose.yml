version: "3.8"

services:
  bot:
    build: .
    container_name: dicerollerbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/dnd_bot.db
    volumes:
      - ./data:/app/data
